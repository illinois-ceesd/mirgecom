# Useful development packages.
# Install them with 'pip install -r requirements_dev.txt'.

memory-profiler
jedi
pudb
pytest-pudb
pyprof2calltree
pyinstrument

git+ssh://github.com/vmprof/vmprof-python.git; platform_machine == "x86_64"
